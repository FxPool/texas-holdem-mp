<view class="ring-wrap" wx:if="{{totalMs > 0}}">
  <view
    class="ring ring-anim"
    style="--total: {{totalMs}}ms; --elapsed: -{{elapsedMs}}ms;"
  ></view>
  <view class="ring-inner"></view>
</view>
