.ring-wrap {
  position: absolute;
  inset: 0;
  pointer-events: none;
}

.ring {
  position: absolute;
  inset: 0;
  border-radius: 50%;
  transition: background 0.2s linear;
}

.ring-inner {
  position: absolute;
  inset: 6rpx;
  border-radius: 50%;
  background: transparent;
}
